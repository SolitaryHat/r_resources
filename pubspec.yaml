name: flutter_resources
description: A new Flutter package.
version: 0.0.1
author:
homepage:

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  build: ^1.6.0
  path: ^1.7.0
  glob: ^1.2.0
  yaml: ^2.2.1
  meta: ^1.2.4

dev_dependencies:
  flutter_test:
    sdk: flutter
  build_test: ^1.3.2  